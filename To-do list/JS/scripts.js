var captar = "";

function chamar() {
        e.preventDefault();
        captar = document.getElementById("texto").value;
        document.getElementById("digito").innerHTML = captar;
};

// Meu codigo esta com erro no tipo de entrada de dados, tirei um print